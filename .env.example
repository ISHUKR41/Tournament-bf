# Tournament Registration Website - Environment Variables

# No environment variables needed for basic deployment!
# This application uses file-based storage by default

# Optional: If you want to use Vercel KV (Redis) for better real-time sync
# Uncomment and add these after creating a Vercel KV database
# KV_REST_API_URL=your_kv_url
# KV_REST_API_TOKEN=your_kv_token

# Production URL (automatically set by Vercel)
# NEXT_PUBLIC_APP_URL=https://your-domain.vercel.app
